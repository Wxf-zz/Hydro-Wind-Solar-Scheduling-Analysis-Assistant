# 水风光调度智能分析助手使用说明

## 一、启动助手

### 方式 A：双击启动

1. 从 GitHub Release 下载 ZIP 文件并解压。
2. 确认电脑已安装 Python 3.12。
3. 双击文件夹里的 `start.bat`。
4. 第一次启动会自动创建虚拟环境并安装依赖，可能需要几分钟和网络连接。
5. 看到启动提示后，在浏览器打开 `http://localhost:8501`。

启动脚本只负责安装依赖和启动本地页面，不会上传项目文件。

### 方式 B：手动启动

在项目根目录打开 PowerShell，依次执行：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m streamlit run app.py
```

关闭运行窗口即可停止助手。

## 二、准备调度结果 CSV

在页面上传的 CSV 必须满足以下要求：

- 使用 GBK 编码保存。
- 包含 365 行数据。
- “时段序号”必须是连续的整数 1-365。
- 第一行列名和顺序必须完全如下：

```text
时段序号
水电出力
风光出力
弃风光率（=弃风光量/风光出力）
出力不足率（=出力不足/负荷）
负荷
水电效率
发电流量
弃水率
水量偏差率
计划出库
实际出库
```

其中弃风光率、出力不足率和弃水率应位于 0-1，水量偏差率应位于 -1 到 1。上传前请确认数据已经脱敏，并且允许用于本地分析。

## 三、查看分析结果

上传并通过校验后，可以在“调度分析”页面查看：

1. 最大功率平衡绝对残差、功率不守恒天数、供电不足天数和异常记录数。
2. 出力与负荷、新能源消纳、流量、水量偏差和水电出力系数等图表。
3. 每条异常的发生时段、规则、实际值、阈值和解释。

当前固定规则包括：功率不守恒、存在供电不足、高弃风光、高弃水和计划执行偏差较大。

## 四、配置大模型

大模型不是必需项。没有配置时，CSV 校验、指标、图表和异常识别仍然可以使用。

本项目支持任意提供 OpenAI Chat Completions 兼容接口的模型服务。请在启动前，在同一个 PowerShell 窗口设置环境变量。

### 通用配置

```powershell
$env:LLM_API_KEY = "你的 API Key"
$env:LLM_BASE_URL = "https://你的服务地址/v1"
$env:LLM_MODEL = "你的模型名称"
.\start.bat
```

### OpenAI 配置

```powershell
$env:OPENAI_API_KEY = "你的 OpenAI API Key"
$env:OPENAI_MODEL = "你的模型名称"
# 如果使用兼容代理，再设置：
# $env:OPENAI_BASE_URL = "https://你的代理地址/v1"
.\start.bat
```

### DeepSeek 快捷配置

```powershell
$env:DEEPSEEK_API_KEY = "你的 DeepSeek API Key"
.\start.bat
```

`LLM_*` 配置优先于 `OPENAI_*` 配置。DeepSeek 快捷配置会使用项目内置的默认接口和模型；其他服务通常需要同时提供密钥、接口地址和模型名称。

大模型服务必须兼容 OpenAI 的 Chat Completions 请求格式。不同服务的模型名称、接口地址和计费规则请以服务商说明为准。

## 五、使用本地知识库

公开仓库和 Release 不包含 CSV 或 PDF 原文。这样可以避免把用户数据和未确认授权的资料公开发布。

如果要使用“知识问答”以及带资料依据的报告：

1. 阅读 `knowledge_base/README.md`。
2. 从其中列出的公开页面取得允许本地使用的 PDF。
3. 按清单中的原文件名放入 `knowledge_base/sources/`。
4. 重新启动助手。

没有本地 PDF 时，页面会给出提示，CSV 分析仍然可用。

## 六、生成报告

1. 在“分析报告”页面确认报告依据和当前大模型配置。
2. 点击“生成 Markdown 报告”。
3. 检查报告预览。
4. 点击下载按钮保存 Markdown 文件。

报告中的指标、异常和数值由 Python 生成；大模型只负责补充定性文字。若没有本地知识库，报告会明确显示没有资料依据。

## 七、常见问题

### 双击 `start.bat` 后窗口马上关闭

在项目文件夹地址栏输入 `powershell` 打开 PowerShell，再执行：

```powershell
.\start.bat
```

根据终端提示安装 Python 或检查依赖安装错误。

### 提示找不到 Python

请安装 Python 3.12，并在安装时勾选“Add Python to PATH”。安装后重新打开终端。

### 页面显示端口被占用

关闭已经运行的 Streamlit 窗口，或在手动启动时使用其他端口：

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py --server.port 8502
```

然后打开 `http://localhost:8502`。

### 大模型无法调用

检查当前 PowerShell 窗口是否设置了正确的密钥、接口地址和模型名称。确认服务商提供的是 OpenAI Chat Completions 兼容接口。密钥不会由项目保存。

### CSV 校验失败

重点检查文件编码是否为 GBK、是否正好有 365 行，以及 12 个列名和顺序是否完全一致。

## 八、隐私与安全

- 不要把 API Key 写入代码、README、批处理文件或 Git。
- 不要把未脱敏或未经授权的调度数据放入公开仓库。
- 使用云端大模型时，请根据所选服务商的政策确认是否可以发送相关业务内容。
