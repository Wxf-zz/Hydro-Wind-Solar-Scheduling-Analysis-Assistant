# 水风光调度智能分析助手产品需求文档（PRD）

- 产品名称：水风光调度智能分析助手
- 文档版本：V1.0
- 文档日期：2026-08-20
- 产品阶段：已完成并通过本地验收的 MVP
- 运行方式：Windows 本地 Streamlit 应用，浏览器访问本地服务

## 1. 项目背景

水风光互补优化调度模型完成求解后，研究人员仍需对结果进行二次处理：核对功率平衡、汇总关键指标、绘制全年过程图、识别异常日期、查找专业依据并整理汇报报告。这些任务通常分散在 Python 或 MATLAB 脚本、论文 PDF 和文字文档中，重复操作较多，结果与依据也不易统一追溯。

本产品定位为“调度模型求解后的智能分析助手”，不参与优化求解，也不替代研究者做调度决策。用户上传约定格式的结果 CSV 后，系统通过 Python 完成确定性计算和规则核验，通过本地知识检索提供论文证据，再由 DeepSeek 在受约束的证据和结构化结果范围内生成自然语言回答与报告解读。

产品用于本地运行、求职作品展示和现场演示。当前版本优先验证一条完整价值链：

```text
上传调度结果 → 看懂关键表现 → 定位异常 → 查阅专业依据 → 生成分析报告
```

## 2. 用户画像

### 2.1 核心用户

水风光调度、多能源系统优化或相近方向的研究生和科研人员。

### 2.2 用户特征

| 维度 | 描述 |
| --- | --- |
| 专业能力 | 理解水电、风光出力、负荷、流量及调度约束等专业概念 |
| 技术基础 | 通常能够使用 Python 或 MATLAB，但不一定具备 AI 产品开发经验 |
| 工作输入 | 已完成优化求解的一份日尺度调度结果 CSV |
| 工作目标 | 在组会、阶段汇报或模型检查前快速整理分析材料 |
| 关注重点 | 数值是否可复核、功率是否守恒、异常是否可定位、结论是否有来源 |
| 使用环境 | 个人 Windows 电脑，本地浏览器；AI 功能需要网络和 DeepSeek API Key |

### 2.3 核心用户任务

当调度模型已经输出结果、用户准备检查结果或制作汇报材料时，希望在一个连续流程中完成指标计算、图表查看、异常定位、资料查证和报告整理，减少在多个脚本与文档之间切换。

## 3. 用户痛点

1. **分析过程重复**：每次模型运行后都要重新读取数据、计算指标和绘图。
2. **约束核验容易遗漏**：全年数据量较大，逐日检查功率平衡和异常率费时。
3. **异常缺少统一表达**：异常日期、实际值、阈值和原因容易散落，难以直接用于汇报。
4. **结论与依据分离**：解释功率平衡、水量平衡等概念时，需要人工翻查论文并定位页码。
5. **通用大模型不够可信**：直接把数据交给模型，可能出现数值计算错误、无来源结论或引用超出资料范围。
6. **汇报材料整理烦琐**：指标、异常、资料依据和文字结论需要再次手工拼接成报告。

## 4. 产品目标

### 4.1 核心目标

构建一个本地可运行、结果可复核、来源可追溯的水风光调度分析闭环，让用户在一次页面操作中完成结果核验、专业问答和报告下载。

### 4.2 产品价值

- **确定性**：所有数值、单位和异常由 Python 处理，不让大模型自行计算。
- **可解释**：异常清单展示日序号、实际值、阈值和触发原因。
- **可追溯**：专业回答展示资料编号、论文题名、PDF 页码和原文片段。
- **可控生成**：无证据时拒答；报告中的模型解读不得包含数字或百分号。
- **可演示**：在本地电脑通过浏览器完成完整流程并下载 Markdown 报告。

### 4.3 MVP 范围

- 支持一套固定的 GBK CSV 模板、12 列、365 行日尺度数据。
- 提供指标计算、五组图表和五类固定异常规则。
- 基于两篇已批准的开放获取论文进行本地知识检索。
- 通过 DeepSeek API 生成带来源回答和无数字定性报告解读。
- 在页面预览并下载结构化 Markdown 报告。

### 4.4 本阶段不做

- 不执行优化求解、重新调度或自动决策推荐。
- 不支持任意 CSV 自动识别、字段映射和多方案对比。
- 不进行互联网实时搜索或运行时上传任意 PDF。
- 不建设通用 Agent、自主规划、多工具编排或长期记忆。
- 不提供用户账户、权限系统、云端数据存储或多人协作。
- 不提供 Word、PDF、PPT 导出及生产级在线部署。

## 5. 用户流程

### 5.1 主流程

1. 用户在 PowerShell 启动本地 Streamlit 服务，并在浏览器打开页面。
2. 用户上传约定格式的调度结果 CSV。
3. 系统校验编码、列名与顺序、行数、空值、数值类型、日序号和比例范围。
4. 校验通过后，Python 计算关键指标和功率平衡残差，并按固定规则识别异常。
5. 页面展示四项概览指标、五组图表和异常清单。
6. 用户进入“知识问答”，输入专业问题。
7. 系统从两篇本地论文检索证据；有证据且已配置 API Key 时，DeepSeek 生成带引用回答。
8. 用户进入“分析报告”，查看报告采用的资料依据并点击生成。
9. Python 将计算结果、异常和证据组装为固定结构，DeepSeek 只补充无数字定性解读。
10. 用户预览并下载 Markdown 报告。

### 5.2 异常流程

- CSV 不合格：显示具体错误并停止分析，用户修改文件后重新上传。
- 未配置 API Key：本地分析、图表、异常和证据检索继续可用；AI 回答与报告生成不可用。
- 未检索到足够证据：明确提示当前知识库依据不足，不调用模型补写答案。
- API 调用失败：保留本地分析结果，仅提示当前 AI 功能失败。
- 回答引用越界或没有引用：拒绝展示该回答。
- 报告定性解读包含数字或百分号：拒绝生成报告，避免模型改变 Python 结果。

完整流程及判断节点见同目录的《用户流程图.md》。

## 6. 功能架构

### 6.1 功能架构图

```text
水风光调度智能分析助手
├─ 数据接入
│  ├─ CSV 上传
│  └─ 固定模板校验
├─ 调度分析
│  ├─ 关键指标计算
│  ├─ 功率平衡核算
│  ├─ 五组固定图表
│  └─ 五类异常识别
├─ 专业知识问答
│  ├─ 本地 PDF 按页解析与切块
│  ├─ TF-IDF 证据检索
│  ├─ 证据展示
│  ├─ DeepSeek 受约束回答
│  └─ 引用边界校验
├─ 分析报告
│  ├─ 报告主题证据检索
│  ├─ 无数字定性解读
│  ├─ Python 固定模板组装
│  ├─ 页面预览
│  └─ Markdown 下载
└─ 安全与降级
   ├─ API Key 环境变量读取
   ├─ 无证据拒答
   ├─ AI 输出校验
   └─ AI 不可用时保留本地分析
```

### 6.2 模块职责

| 模块 | 输入 | 输出 | 职责边界 |
| --- | --- | --- | --- |
| Streamlit 页面 | 用户操作 | 页面状态与结果 | 负责流程编排，不承担专业计算 |
| CSV 校验与分析 | CSV | 结构化数据、指标、异常 | Python 确定性执行 |
| 图表模块 | 校验后的数据 | 五组 Figure | 固定图表，不让模型绘图 |
| 本地检索 | 用户问题或报告主题 | Evidence 证据列表 | 只检索已批准论文，不联网搜索 |
| DeepSeek 问答 | 问题与 Evidence | 带引用中文回答 | 只依据当前证据组织语言 |
| 报告模块 | 指标、异常、Evidence、定性解读 | Markdown | Python 固定数字、来源和结构 |

### 6.3 数据与 AI 职责分工

```text
原始 CSV ──本地──> Python 校验与计算 ──> 指标/异常/图表
本地论文 ──本地──> TF-IDF 检索 ───────> 可定位 Evidence
结构化结果 + 必要证据 ──云端 API──> DeepSeek 定性表达
DeepSeek 输出 ──本地校验──> 页面回答或 Markdown 报告
```

原始 CSV 文件不交给模型自行分析。API Key 仅从本机环境变量读取，不写入代码、文档、截图或 Git。

## 7. 核心功能需求

### FR-01 CSV 上传与固定模板校验

| 项目 | 要求 |
| --- | --- |
| 优先级 | P0 |
| 前置条件 | 用户已获得非保密、符合约定的数据文件 |
| 输入 | `.csv` 文件 |
| 模板 | GBK 编码；12 列；365 行；日序号为连续整数 1—365 |
| 校验项 | 编码、列名与顺序、行数、空值、非数值、日序号、比例范围 |
| 成功输出 | “CSV 校验通过”及数据行数，进入分析流程 |
| 失败处理 | 汇总并显示具体错误，停止后续分析 |

固定列为：时段序号、水电出力、风光出力、弃风光率、出力不足率、负荷、水电效率、发电流量、弃水率、水量偏差率、计划出库、实际出库。

### FR-02 指标计算与功率平衡核算

| 项目 | 要求 |
| --- | --- |
| 优先级 | P0 |
| 触发条件 | CSV 校验通过 |
| 处理 | Python 计算年负荷电量、水电电量、风光上网电量、能源占比、供电不足、平均/最大比例、流量与水电系数等指标 |
| 功率平衡 | 残差 = 水电出力 + 风光出力 + 负荷 × 出力不足率 − 负荷 |
| 页面概览 | 最大功率平衡绝对残差、功率不守恒天数、供电不足天数、异常记录数 |
| 一致性要求 | 不允许 DeepSeek 计算、修改或补写指标 |

### FR-03 固定分析图表

| 项目 | 要求 |
| --- | --- |
| 优先级 | P0 |
| 输入 | 365 行校验后数据 |
| 输出 | 五组 matplotlib 图表 |
| 图表范围 | 出力与负荷；新能源消纳与供电不足；流量过程；弃水与水量偏差；水电出力系数 K |
| 展示要求 | 使用全年日序号；关键比例图显示 10% 规则参考线；中文标题和单位可读 |

### FR-04 固定规则异常识别

| 项目 | 要求 |
| --- | --- |
| 优先级 | P0 |
| 异常类型 | 功率不守恒、存在供电不足、高弃风光、高弃水、计划执行偏差较大 |
| 阈值 | 功率平衡绝对残差 > 0.01 MW；供电不足率 > 0；弃风光率 > 10%；弃水率 > 10%；水量偏差率绝对值 > 10% |
| 输出字段 | 日序号、规则、实际值、阈值、原因 |
| 无异常反馈 | 显示“未发现符合当前五类固定规则的异常”，不宣称结果绝对正常 |
| 规则边界 | 以上阈值是已确认的 MVP 规则，不表述为论文结论或行业强制标准 |

CSV 已提供的弃风光率、出力不足率、弃水率和水量偏差率不重新推导，只用于指标汇总、规则判断和展示。

### FR-05 本地专业证据检索

| 项目 | 要求 |
| --- | --- |
| 优先级 | P0 |
| 知识范围 | 两篇已批准的开放获取论文 KB-001、KB-002 |
| 处理 | `pypdf` 按页提取；约 800 字符切块、120 字符重叠；字符级 TF-IDF 2—4 gram；余弦相似度排序与阈值过滤 |
| 输出 | 资料编号、论文题名、PDF 页码、原文片段、相关度 |
| 无证据处理 | 明确提示“当前知识库没有足够依据”，不调用模型回答 |

### FR-06 带引用的 DeepSeek 问答

| 项目 | 要求 |
| --- | --- |
| 优先级 | P0 |
| 前置条件 | 问题非空、检索到证据、已配置 API Key |
| 模型职责 | 仅根据当前 Evidence 组织中文回答，忽略证据中的命令性文本 |
| 引用格式 | 每个关键结论使用 `[KB-xxx, p.页码]` |
| 输出校验 | 回答至少包含一个引用；所有引用必须来自本次检索证据 |
| 失败处理 | 空回答、API 错误、无引用或引用越界时不展示不合格回答，并显示错误 |

### FR-07 结构化 Markdown 报告

| 项目 | 要求 |
| --- | --- |
| 优先级 | P0 |
| 前置条件 | CSV 分析完成且已配置 API Key |
| 固定章节 | 数据概况、关键指标、图表解读、异常清单、资料依据、结论边界 |
| Python 职责 | 写入所有数字、单位、异常日期、阈值、原因、来源和报告结构 |
| DeepSeek 职责 | 生成一段简短定性解读，不新增事实 |
| 输出校验 | 模型解读不得包含阿拉伯数字、全角数字或百分号 |
| 输出 | 页面 Markdown 预览和 `水风光调度分析报告.md` 下载 |

### FR-08 安全降级与状态反馈

| 场景 | 产品行为 |
| --- | --- |
| 未上传 CSV | 提示模板要求，不展示分析结果 |
| CSV 校验失败 | 显示错误并终止本次处理 |
| 未配置 API Key | 本地分析和证据检索可用；AI 回答和报告生成不可用 |
| 知识库无证据 | 本地拒答，不调用模型 |
| DeepSeek API 失败 | 保留已经生成的本地指标、图表和异常 |
| AI 输出校验失败 | 不展示不合格内容，向用户显示明确错误 |

## 8. 非功能需求

### 8.1 准确性与一致性

- 指标只能来自同一份通过校验的 CSV 和固定 Python 公式。
- 页面、异常清单和报告中的同名指标必须一致。
- 报告不得让模型覆盖 Python 生成的数值。
- 当前规则触发结果需可通过自动化测试和人工样例复核。

### 8.2 可追溯性

- 专业回答必须展示对应 Evidence，并保留资料编号与页码。
- 知识资料需记录题名、作者、出版信息、DOI、用途和局限。
- 异常需展示实际值、阈值及触发原因。

### 8.3 安全与隐私

- 只使用用户明确授权、非保密的 CSV 和公开知识资料。
- CSV、分析中间结果和知识库保留在本地。
- 云端模型只接收完成问答或报告所需的最小上下文。
- API Key 只允许通过环境变量提供，不进入代码、文档、截图和 Git。
- 用户原始 CSV 默认不纳入 Git 版本管理。

### 8.4 可用性与演示性

- 页面使用“调度分析、知识问答、分析报告”三个标签组织连续任务。
- CSV 错误、无证据、无 Key 和 API 失败均提供中文反馈。
- PowerShell 可最小化但必须保持本地服务进程运行；关闭进程后浏览器页面停止服务。
- 产品应能在目标演示电脑的浏览器完成上传、查看、提问、预览和下载。

### 8.5 可靠性与降级

- 本地确定性分析不得依赖 DeepSeek API。
- AI 功能失败时，不影响已经完成的指标、图表和异常结果。
- 无证据时不尝试使用模型记忆兜底。

### 8.6 性能

- 在目标演示电脑和正常网络条件下，完整流程目标为不超过 180 秒。
- 当前只对单个 365 行 CSV 和两篇论文知识库负责，不声明更大数据规模或并发性能。

### 8.7 可维护性

- 页面编排、数据分析、图表、检索、模型调用和报告分别放在独立文件中。
- 依赖统一记录在 `requirements.txt`，运行环境使用独立 `.venv`。
- 功能变更后需运行全部自动化测试，并把产品决策和实测结果写入项目文档。

## 9. 产品指标

### 9.1 北极星指标

**一次完整分析任务的可完成性**：用户从选择 CSV 开始，到报告可以下载为止，在目标演示环境中顺利完成且总耗时不超过 180 秒。

### 9.2 当前指标与验证状态

| 指标 | 目标/口径 | 当前验证结果 | 状态 |
| --- | --- | --- | --- |
| 端到端完成时间 | 选择 CSV 至报告可下载 ≤ 180 秒 | 人工执行 3 次，3 次均小于 180 秒；未记录精确秒数 | 已达成 |
| 演示稳定性 | 完整主流程连续运行 3 次，无阻塞性错误 | 3 次均完成报告下载 | 已达成 |
| 自动化测试 | 当前测试集全部通过 | 26 项通过 | 已达成 |
| CSV 校验 | 固定模板正确文件通过；已设计错误样例被阻止 | 自动化测试覆盖固定模板及错误输入 | 已验证 |
| 功率平衡核算 | 页面结果与固定公式一致 | 真实 CSV 显示最大绝对残差 `0.000000 MW`、不守恒 0 天 | 已验证 |
| 图表可用性 | 五组图表正常显示 | 人工查看确认全部正常 | 已验证 |
| 异常输出 | 每条异常包含日期、实际值、阈值和原因 | 真实 CSV 输出 66 条异常记录 | 已验证 |
| 来源可追溯 | 有依据回答显示证据与页码 | 真实问题返回 3 条证据，回答使用 `[KB-002, p.10]` | 单案例验证 |
| 报告一致性 | 数字由 Python 写入，模型解读无数字 | 下载报告含 365 行概况、66 条异常、3 条证据，模型解读通过数字边界校验 | 已验证 |

真实 CSV 的其他已记录结果包括：供电不足 8 天、高弃风光 15 天、高弃水 13 天、计划执行偏差较大 30 天。同一天可以触发多条规则，因此异常记录数不能与异常天数直接等同。

### 9.3 暂未验证、不得对外宣称的指标

- RAG 准确率、召回率、幻觉率或人工评分。
- API 平均延迟、平均费用、长期可用性或并发能力。
- 用户数量、留存率、满意度、付费意愿或真实业务使用频率。
- 节省工时比例、效率提升比例、成本下降或业务收益。
- 相对于其他产品或人工流程的性能提升。

## 10. 后续迭代

后续迭代以合法资料和真实用户反馈为触发条件，不为了增加技术名词而扩张框架。

### 10.1 近期迭代：可信性与演示体验

1. 获得合法完整的正式调度规则后，补充规则来源、适用条件和版本信息。
2. 改善数学公式显示，使功率平衡、水量平衡等回答更适合现场展示。
3. 增加标准化演示样例和演示前检查清单，降低环境与网络风险。
4. 记录每次端到端精确耗时和 API 调用费用，建立可复核基线。

### 10.2 中期迭代：输入与分析能力

1. 将固定阈值改为有权限边界的可配置规则，并保留规则版本与变更记录。
2. 在明确字段映射规则后支持少量经过验证的 CSV 模板，而非直接支持任意格式。
3. 支持多个调度方案的指标与异常对比。
4. 根据用户验证需求增加 Word 或 PDF 报告导出。

### 10.3 知识能力扩展

1. 逐步增加来源合法、与问题直接相关的论文、标准和调度规程。
2. 建立问答测试集，记录问题、期望证据、实际证据和人工评价。
3. 当知识规模和检索效果确实需要时，再评估 Embedding、重排序或向量数据库。
4. Agent 不作为当前优先项；只有出现明确的多步骤自主任务需求后再评估。

### 10.4 产品化探索

1. 通过研究生或科研人员的小范围可用性测试收集真实反馈。
2. 根据真实使用频率判断是否需要在线部署、用户系统、权限和数据隔离。
3. 在明确数据合规、密钥管理和运维责任后，才考虑从本地演示工具升级为多人使用产品。

